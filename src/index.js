/**
 * all exports of store folder
 */

import App from './App';
// it a future it should be replaced with react-native-config for better build automatization
import config from '../appconfig'; 

export {
    config
};
export default App;
